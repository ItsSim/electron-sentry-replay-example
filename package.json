{
  "name": "electron-sentry-replay-example",
  "version": "1.0.0",
  "description": "",
  "main": "./src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron ./src",
    "build-win": "electron-packager . electron-sentry-replay-example --overwrite --platform=win32 --arch=x64 --out=release --ignore=/node_modules --ignore=/release",
    "build-mac": "electron-packager . electron-sentry-replay-example --overwrite --platform=darwin --arch=universal --out=release --ignore=/node_modules --ignore=/release"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@electron/packager": "^18.1.3",
    "electron": "^29.1.4"
  }
}
